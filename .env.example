# RentEase Kenya — environment configuration
# Copy to .env and set your PostgreSQL connection string. This is the ONLY
# environment variable the application requires.

DATABASE_URL="postgresql://user:password@localhost:5432/rentalhub?schema=public"
